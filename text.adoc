= VTL AsciiDoc Test Document
== Section One

Это первый параграф с *жирным текстом* и _курсивом_, а также `mono` фрагментом.

Здесь у нас [line-through]#зачёркнутый текст# внутри обычного параграфа.

== Section Two

* Первый пункт списка
* Второй пункт с *выделением*
** Вложенный пункт

. Нумерованный пункт
. Ещё один

[source,c]
----
#include <stdio.h>
int main(void) { return 0; }
----

// Это комментарий, который не должен попасть в вывод.

Финальный параграф со *жирным*, _курсивом_, `mono` и [line-through]#зачёркиванием#.
